// WorkflowRouter: entfernt — ersetzt durch Ops Center
// Kept as stub to avoid import errors

export default function WorkflowRouter() {
  return (
    <div style={{ padding: 40, color: '#64748b', textAlign: 'center' }}>
      Workflow wurde durch das Ops Center ersetzt.
    </div>
  );
}
